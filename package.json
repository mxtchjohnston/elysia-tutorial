{
  "name": "hi-elysia",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "build": "rm bin/server && bun build --compile --minify-whitespace --minify-syntax --target bun --outfile bin/server ./src/index.ts"
  },
  "dependencies": {
    "@elysiajs/swagger": "^1.1.5",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}